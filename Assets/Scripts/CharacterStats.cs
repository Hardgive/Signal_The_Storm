using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterStats : MonoBehaviour
{

    // ѕолучаем Id снар€жени€ персонажа дл€ экипированных слотов, чтобы потом их отобраить на модели
    public int suitId;
    public int headId;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void CharStats()
    {
        int suitEquipment = suitId;
        int headEquipment = headId;
        int health = 100;

        
    }
}
